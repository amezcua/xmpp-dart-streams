name: xmpp_dart_streams
description: A simple stream based XMPP client library .
version: 0.1.0

publish_to: 'none'

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  xml: ^4.1.0
  tuple: ^1.0.3
  equatable: ^1.1.1
  rxdart: ^0.24.0
  uuid: ^2.0.4
  crypto: ^2.1.4
  password_hash: ^2.0.0
  hex: ^0.1.2
  unorm_dart: ^0.1.2

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.14.2
